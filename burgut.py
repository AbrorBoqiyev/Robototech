# Burgut(2)'s Python code

import Entry

def when_start():
    while True:
        Entry.wait_for_sec(0.1)
        Entry.change_shape_to("next")

def when_start():
    while True:
        Entry.move_to_direction(3)
        Entry.bounce_on_edge()